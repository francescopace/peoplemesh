apiVersion: v1
kind: Service
metadata:
  name: {{ include "peoplemesh.fullname" . }}
  labels:
    {{- include "peoplemesh.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "peoplemesh.selectorLabels" . | nindent 4 }}
