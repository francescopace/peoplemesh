import { renderSkillsCatalogPanel } from "../components/skills-catalog-panel.js";

export async function renderSkillsCatalog(container, options = {}) {
  return renderSkillsCatalogPanel(container, options);
}
